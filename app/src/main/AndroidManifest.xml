<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testgooglelogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="chat_real_time.MessageActivity"></activity>
        <activity android:name="login_logout_register.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.timphongtro.MainActivity" />
        <activity android:name="login_logout_register.RegisterActivity" />
        <activity android:name="login_logout_register.LogoutActivity" />
        <activity android:name="post_room.PostRoomActivity" />
    </application>

</manifest>